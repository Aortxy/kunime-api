/**
 * @typedef {Object} StreamMirror
 * @property {string} quality
 * @property {string} server
 * @property {string} token
 */

/**
 * @typedef {Object} EpisodeStreams
 * @property {string} episode_slug
 * @property {StreamMirror[]} streams
 */

/**
 * @typedef {Object} ResolvedStream
 * @property {string} [quality]
 * @property {string} [server]
 * @property {string} url
 */
export const StreamModels = {}; // JSDoc only models
